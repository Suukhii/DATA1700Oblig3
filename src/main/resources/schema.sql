CREATE TABLE TICKET (
    id INT AUTO_INCREMENT PRIMARY KEY,
    film VARCHAR(255),
    quantity INT,
    first_name VARCHAR(255),
    last_name VARCHAR(255),
    telephone VARCHAR(20),
    email VARCHAR(255)
);

